@model IEnumerable<VonderkCRUD.Trabajo>

@{
    ViewBag.Title = "Index";
}


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Imagen)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        string s = item.Imagen;
        int index = s.IndexOf(',');
        string first = null;
        if (index != -1)
        {
            first = s.Substring(0, index);
        }
        else
        {
            first = s;
        }


        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                <img src="~/Images/Trabajos/@item.NombreCorregido/@first" width="100" height="100" />
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>*@




<section id="trabajos">
    <div class="content">
        <h1 data-easy-reveal="fade-in-up">trabajos</h1>


        <div class="product-list" data-easy-reveal="fade-in-up">

            <ul>

                @foreach (var item in Model)
                {

                    string s = item.Imagen;
                    int index = s.IndexOf(',');
                    string first = s.Substring(0, index);


                    <li class="@Html.DisplayFor(modelItem => item.NombreCorregido)"><a href="@Url.Action("Details", "Trabajos", new {id= item.ID})"><img src="~/Images/Trabajos/@item.NombreCorregido/@first" width="100" height="100" /><span>@Html.DisplayFor(modelItem => item.Nombre)</span></a></li>

                }




            </ul>


        </div>

    </div>
</section>

